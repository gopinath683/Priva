kind = "web"
previewPath = "/"
title = "Priva"
version = "1.0.0"
id = "artifacts/priva"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20497

[services.development]
run = "pnpm --filter @workspace/priva run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/priva", "run", "build" ]
publicDir = "artifacts/priva/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20497"
BASE_PATH = "/"
